# .github/workflows/netlify-deploy.yml
name: Netlify Deploy 🚀

on:
  workflow_call: {} # 👈 also change to workflow_call

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Testing
        run: echo 'Hello world1234'
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*
      - name: Deploy logic
        run: echo 'Deploy to Netlify'
