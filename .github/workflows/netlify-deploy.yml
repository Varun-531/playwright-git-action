name: Netlify Deploy

on:
  workflow_run:
    workflows: ["Playwright Tests 🏃💨"] # must match `name:` in playwright.yml
    types:
      - completed

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: lts/*

      - name: Install dependencies
        run: echo 'Hello world'
